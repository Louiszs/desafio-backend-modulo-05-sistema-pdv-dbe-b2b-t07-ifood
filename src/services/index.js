const {
  getUserAndReturn,
  createUserAndReturn,
  loginUserAndReturn,
  sendEmail,
  updateUserAndConfirm
} = require('./user');

module.exports = {
  getUserAndReturn,
  createUserAndReturn,
  loginUserAndReturn,
  sendEmail,
  updateUserAndConfirm
}
