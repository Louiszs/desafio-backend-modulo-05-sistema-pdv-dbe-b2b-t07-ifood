const {
  getUserAndReturn,
  createUserAndReturn,
  loginUserAndReturn,
  sendEmail
} = require('./user');

module.exports = {
  getUserAndReturn,
  createUserAndReturn,
  loginUserAndReturn,
  sendEmail
}
